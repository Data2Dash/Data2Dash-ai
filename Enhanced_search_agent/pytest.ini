[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no network calls)
    integration: Tests that hit real APIs
    coverage: Topic recall / coverage measurement tests
